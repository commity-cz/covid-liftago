{
  "name": "covid-liftago",
  "version": "1.0.0",
  "scripts": {
    "predeploy:staging": "cd frontend && yarn install && yarn build",
    "predeploy:production": "cd frontend && yarn install && yarn build",
    "deploy:staging": "firebase deploy -P default",
    "deploy:production": "firebase deploy -P production"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/commity-cz/covid-liftago.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/commity-cz/covid-liftago/issues"
  },
  "homepage": "https://github.com/commity-cz/covid-liftago#readme",
  "devDependencies": {
    "firebase-tools": "^7.15.1"
  }
}
